specVersion: "v1"
serviceId: "messenger-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "messenger-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
